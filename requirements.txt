# Ultron runtime dependencies

# HTTP (FastAPI server)
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0

# DashScope embedding API + OpenAI-compatible LLM
dashscope>=1.14.0
openai>=1.0.0

# Local embedding backend (ultron.core.embeddings, backend=local)
transformers>=4.51.0
sentence-transformers>=2.7.0

# Token counting (UltronConfig llm_*_tokens, ULTRON_LLM_*)
tiktoken>=0.5.0

# Optional ~/.ultron/.env loading (ultron.config.load_ultron_dotenv)
python-dotenv>=1.0.0

# Scripts (e.g. import_zclawbench_trajectories.py)
tqdm>=4.66.0

# Authentication (user login/registration + JWT sessions)
PyJWT>=2.8.0
bcrypt>=4.0.0

# PII sanitization for persisted memory (ultron.utils.sanitizer.DataSanitizer)
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.362